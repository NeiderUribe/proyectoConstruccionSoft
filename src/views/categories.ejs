<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <header class="header admin-header">
        <div class="container">
            <div class="header-content">
                <div class="logo"><h1>🍔 Panel Administrador</h1></div>
                <nav class="nav">
                    <a href="/admin" class="nav-link">← Dashboard</a>
                    <a href="/admin/categories" class="nav-link active">Categorías</a>
                    <a href="/logout" class="nav-link btn-danger">Cerrar Sesión</a>
                </nav>
            </div>
        </div>
    </header>

    <main class="main-content admin-main">
        <div class="container">
            <div class="page-header">
                <h1>Gestión de Categorías</h1>
                <a href="/admin/categories/create" class="btn btn-primary">+ Crear Categoría</a>
            </div>

            <% if (error) { %>
                <div class="alert alert-error"><strong>Error:</strong> <%= error %></div>
            <% } %>

            <% if (success) { %>
                <div class="alert alert-success"><strong>Éxito:</strong> <%= success %></div>
            <% } %>

            <div class="table-container">
                <% if (categories && categories.length > 0) { %>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nombre</th>
                                <th>Descripción</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% categories.forEach(cat => { %>
                                <tr>
                                    <td><%= cat.id_category %></td>
                                    <td><%= cat.name %></td>
                                    <td><%= cat.description %></td>
                                    <td class="actions">
                                        <a href="/admin/categories/edit/<%= cat.id_category %>" class="btn btn-sm btn-edit">Editar</a>
                                        <form action="/admin/categories/delete/<%= cat.id_category %>" method="POST" style="display: inline;" onsubmit="return confirm('¿Eliminar esta categoría?');">
                                            <button type="submit" class="btn btn-sm btn-delete">Eliminar</button>
                                        </form>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="empty-state">
                        <p>No hay categorías registradas</p>
                        <a href="/admin/categories/create" class="btn btn-primary">Crear primera categoría</a>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>

